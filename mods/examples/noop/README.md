# No-op package example

This source tree is intentionally inert: it contains no patches, overlays,
plugins or retail game bytes. Copy it to start a package, change the identity,
then add only operations and replacement assets you have tested and may
redistribute. See the repository's `MODDING.md` and
`psxrecomp/docs/MOD_PACKAGES.md` before adding an operation.
